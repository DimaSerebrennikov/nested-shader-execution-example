# Nested shader execution example

This is an example of how to reduce shader size by passing a generated texture.

MaterialToTexture_Renderer - passes a generated texture and accepts a

Chessboard Generatable - a shader that is generated into a texture.